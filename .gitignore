# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log
firebase-login.log

# Functions deps (large, regenerated by CI)
functions/node_modules/
functions/.env
functions/.env.local
functions/.env.*.local

# Backups & local-only
.firebaserc.bak

# OS / editor noise
Thumbs.db
.DS_Store
*.swp

# Don't ship the bat files (they're for local dev only)
# (Keep them — they're handy.) Uncomment if you want to hide them:
# 1-DEPLOY.bat
# 1-LOGIN.bat
